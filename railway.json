{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan optimize:clear && (php artisan queue:work & php artisan serve --host=0.0.0.0 --port=$PORT)"
  }
}
